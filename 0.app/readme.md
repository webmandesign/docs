# Application info

## App folder name

The application folder name must start with a number or a letter for Prepros
(or Pug?) to be able to process the file changes in the app folder.
So, don't start the app folder name with `.` or `_`!

So, the only viable name to put the app folder at the top of folder structure
is using `0.app` name.
