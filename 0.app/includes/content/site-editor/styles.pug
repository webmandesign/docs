block site-editor/styles
	+sub_section( 'Editing theme styles', 'site-editor-styles' )

		+mark( 'nav', 'Appearance', 'Editor', 'Styles' ).is-path-featured

		p
			| "
			include ../../../assets/images/icons/fse-styles.svg
			| Styles" section of site editor allows you to change colors and layout.

		p
			a( href="#site-editor-styles-typography") Typography &darr;
			=' — '
			a( href="#site-editor-styles-colors") Colors &darr;
			=' — '
			a( href="#site-editor-styles-layout") Layout &darr;
			=' — '
			a( href="#site-editor-styles-others") Others &darr;

		+info( 'Section colors', 'h4' )
			p If you want to change colors of a specific section, <a href="#site-editor-template-parts">edit template parts</a> instead.

		+important( 'Typography setup', 'h4' )
			p Although "Styles" section allows you to set up your website typography, the theme provides <strong>advanced harmonious responsive typography setup</strong> tweaking <a href="#typography">just a few theme options</a>. So, there is no need to set up typography here.
			p However, you can still use this section to <a href="https://wordpress.org/documentation/article/the-font-library/">upload your custom fonts</a>.

		+video( 'https://learn.wordpress.org/lesson/customizing-theme-settings-colors-fonts-typography-and-layout/' ) Customizing colors, fonts, and layout

		ol
			li Open Site Editor navigating to #[+mark( 'nav', 'Appearance', 'Editor' )].
			li
				p In Site Editor open "<strong>Styles</strong>" management.
				block site-editor/styles/image/02
					+image( 'core/fse/menu-styles.webp' )

			li
				p In styles manager you can choose to apply predefined theme styles (click "<strong>Browse styles</strong>" for the list of available styles) or tweak the individual style options to your liking.

				block site-editor/styles/typography
					h4#site-editor-styles-typography
						a.heading-anchor( href="#site-editor-styles-typography" )
							include ../../../assets/images/icons/fse-typography.svg
							| Typography
					p Although "Styles" section allows you to set up your website typography, the theme provides <strong>advanced harmonious responsive typography setup</strong> tweaking <a href="#typography">just a few theme options</a>. So, there is no need to set up typography here.
					p However, you can still <strong>use this section to <a href="https://wordpress.org/documentation/article/the-font-library/">upload your custom fonts</a></strong>.

					+video( 'https://learn.wordpress.org/lesson/making-the-most-of-the-font-library/' ) Making the most of the Font Library

				block site-editor/styles/colors
					h4#site-editor-styles-colors
						a.heading-anchor( href="#site-editor-styles-colors" )
							include ../../../assets/images/icons/fse-colors.svg
							| Colors
					block site-editor/styles/colors/top
					p Theme applies a palette of colors throughout the site elements. You can modify each individual color in the palette, modify WordPress default color palette, and add your own custom colors.
					+important( 'Use solid colors', 'h4' )
						p As the theme automatically calculates additional colors from its palette, it is <strong>recommended to use solid colors</strong>, not a semi-transparent ones.
					if true === config.compatibility.fse_colors_elements
						+info( 'Link & element colors', 'h4' )
							p Editing global styles Colors section also allows you to set colors for "Text", "Background", "Link", "Captions", "Button" and "Heading" elements.
							p Please note this is <strong><em>not advisable</em></strong> for this theme. Instead, change the theme color palette accordingly - check the colors information below.
							p Actually, the only "safe", usable option from elements colors setup group is the "<strong>Link</strong>" color setup. If needed, you can tweak that one to your needs.
					block site-editor/styles/colors/bottom
					block site-editor/styles/colors/image
						+image( 'core/fse/styles-colors.webp', 'Editing theme colors' )
					+tip( 'Editing tips', 'h4' )
						p To see the color names, click the "Color options" button (1) and select "Show details" (2).
						block site-editor/styles/colors/tip/image
							+image( 'core/fse/styles-colors-tips.webp' )
						p If you want to revert your color changes, click the "Color options" button (3) and select "Reset colors" (4).

				block site-editor/styles/layout
					h4#site-editor-styles-layout
						a.heading-anchor( href="#site-editor-styles-layout" )
							include ../../../assets/images/icons/fse-layout.svg
							| Layout
					block site-editor/styles/layout/top
					p In "Layout" section it is recommended to tweak <em>content and wide width</em> if needed.
					p Other layout options are set for the best responsiveness across multiple devices. It is not recommended to modify them in this theme.
					block site-editor/styles/layout/bottom
					block site-editor/styles/layout/image
						+image( 'core/fse/styles-layout.webp', 'Modifying layout widths' )

				block site-editor/styles/others
					h4#site-editor-styles-others
						a.heading-anchor( href="#site-editor-styles-others" ) Others
					p Additionally, you can:
					ul
						li tweak styles for each block individually (modify the default <em>appearance of any block</em> to your needs),
						li(style="margin-top:0") set website background (if your theme supports it),
						li(style="margin-top:0") set shadow presets (if your theme supports them).
					p There is also an option to add <strong>CSS styles code</strong> - click the "⫶" next to an eye icon:
					+image( 'core/fse/additional-css.webp' )
